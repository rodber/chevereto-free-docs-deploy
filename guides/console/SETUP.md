# Setup (console)

Clone repository:

```sh
git clone https://github.com/chevere/docs-deploy.git
```

Go to the project:

```sh
cd docs-deploy
```

Install dependencies:

```sh
yarn && composer install
```
